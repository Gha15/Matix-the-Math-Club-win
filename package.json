{
  "name": "matix-the-math-club-windows",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo 'Add your compilation command here' && mkdir dist" 
  }
}
